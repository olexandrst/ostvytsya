# 🌳 Голосовий квест-агент парку «Оствиця»

Голосові квести для парку історичної реконструкції
[**«Оствиця»**](https://ostvytsya.com.ua/) (Рівне). Термінал «спить» і **не реагує
ні на що**, доки дитина не промовить кодове слово **«Оствиця»**. Тоді персонаж
оживає, голосом веде квест і відправляє гравців далі.

Персонажі, голоси, кодові слова та самі запитання — **у конфігурації**, тож одним
кодом можна підняти скільки завгодно різних героїв (приклади — Водяник, Повітруля).

Проєкт складається з **двох частин**:

| Частина | Де живе | Модель | Для чого |
|---|---|---|---|
| 📱 **Мобільний застосунок** | Android-телефон у парку, [`mobile/`](mobile/) | **Google Gemini Live** (Native Audio) | власне квест: сон до кодового слова (офлайн-Vosk), розмова, запис сесій |
| 🌐 **Веб-панель** | сервер, [`web/`](web/) | **OpenAI Realtime** або **Gemini Live** | редагувати персонажів, ганяти квест у браузері, бачити всі термінали |

Персонажі — ті самі файли `characters/*.yaml`. Створив у вебі → доступний і в
застосунку.

> 📜 Людиночитний сценарій: [`scripts/quest-domovychok.md`](scripts/quest-domovychok.md)

---

## Як це працює

**Мобільний застосунок** (термінал у парку):

```
        режим СПОКОЮ                              режим КВЕСТУ
 ┌────────────────────────┐   "Оствиця"   ┌──────────────────────────────┐
 │ детектор кодового слова │ ───────────▶ │  Gemini Live  (Native Audio) │
 │ (Vosk, офлайн, тиша,    │              │  • персона + сценарій        │
 │  нічого не коштує)      │ ◀─── reset ──│  • запитання, підказки       │
 └────────────────────────┘  перемога/    │  • судить відповіді          │
        ▲        │            тайм-аут     │  • каже слово → кінець      │
   мікрофон   колонка                      └──────────────────────────────┘
```

Ключ Gemini лежить лише на самому телефоні — застосунок ходить до моделі
напряму, без нашого сервера.

**Веб-панель** (браузер):

```
                          ┌──────────────┐   ephemeral-токен   ┌──────────────┐
 ┌─────────┐ логін/пароль │  наш сервер  │────────────────────▶│    OpenAI    │
 │ браузер │─────────────▶│  (FastAPI)   │  промпт + голос     │  Realtime    │
 │         │              │              │◀══ WebRTC напряму ═▶│              │
 │  🎙 🔊  │              │              │                     └──────────────┘
 │         │◀═ WebSocket ═│    міст      │────────────────────▶┌──────────────┐
 └─────────┘   PCM-аудіо  └──────────────┘   аудіо + промпт    │ Gemini Live  │
                                 │                             └──────────────┘
                          characters/*.yaml
                                 ▲
                    статус раз на 5 хв │ 📱 термінали в парку
```

Ключі **ніколи не потрапляють у браузер**. Для OpenAI сервер видає
короткоживучий ephemeral-токен (звук іде напряму, з мінімальною затримкою);
для Gemini тримає міст і пересилає аудіо сам. В обох випадках промпт і голос
задає сервер — клієнт їх підмінити не може.

Логіку розмови веде модель — це робить діалог природним і дозволяє щедро
зараховувати відповіді (синоніми, відмінки), а не звіряти рядки в коді.

---

## Структура

```
ostvytsya/
├── config.yaml                  # налаштування сервера (сховище персонажів)
├── characters/                  # СПІЛЬНІ сценарії
│   ├── domovychok.yaml          # ПЕРСОНАЖ + СЦЕНАРІЙ (Домовичок, слово «Начувайтеся»)
│   ├── vodyanyk.yaml            # приклад 2-го персонажа (Водяник, інший голос/слово)
│   ├── povitrulya.yaml          # квест на вежі: 8 слів → 4 пари → «РІК»
│   └── derevo.yaml              # три загадки стилем Йоди, слово «Мокоші»
├── scripts/
│   └── quest-domovychok.md      # сценарій у читабельному вигляді
├── domovyk_quest/               # спільний код персонажів (використовує веб)
│   ├── prompt.py                # збирає системну інструкцію зі сценарію
│   ├── config.py                # модель Character/Question
│   ├── characters.py            # файловий бекенд персонажів (CRUD над YAML)
│   ├── characters_pg.py         # бекенд PostgreSQL (опційний, storage.render: yes)
│   └── characters_store.py      # диспетчер: файли чи PostgreSQL
├── web/                         # ВЕБ-ПАНЕЛЬ (OpenAI Realtime / Gemini Live)
│   ├── app.py                   #   FastAPI: сторінки, CRUD-API, видача токенів
│   ├── agents.py                #   реєстр терміналів (звіти від застосунків)
│   ├── auth.py                  #   логін/пароль (pbkdf2), сесійна кука
│   ├── realtime.py              #   ephemeral-токени OpenAI, конфіг сесії
│   ├── gemini_live.py           #   міст браузер ⇄ Gemini Live (WebSocket + PCM)
│   ├── templates/               #   сторінки (вхід, список, редактор, квест, термінали)
│   └── static/                  #   стилі, WebRTC-клієнт і PCM-клієнт для Gemini
├── mobile/                      # ANDROID-ЗАСТОСУНОК (Flutter)
│   ├── lib/quest/               #   транспорти Gemini/OpenAI, аудіо, детектор слова
│   ├── lib/services/            #   налаштування, записи сесій, звіти в панель
│   └── android/…/kotlin/        #   нативне аудіо, телеметрія, сховище
└── requirements-web.txt         # залежності веб-панелі
```

---

## 🌐 Веб-режим (OpenAI Realtime · Google Gemini Live)

Редактор персонажів, голосовий квест просто в браузері та панель усіх
терміналів. Персонажі — ті самі `characters/*.yaml`, що й у застосунку.

### Запуск

```bash
# 1. Залежності
pip install -r requirements-web.txt

# 2. Налаштування — СТВОРИ ФАЙЛ .env (саме .env, а не .env.example!)
cp .env.example .env                # Windows PowerShell: copy .env.example .env
#   OPENAI_API_KEY=...              ← ключ з https://platform.openai.com/api-keys
#   OSTVYTSYA_WEB_USER=admin
#   OSTVYTSYA_WEB_PASSWORD=...      ← пароль для входу
#   OSTVYTSYA_WEB_SECRET=...        ← python -c "import secrets; print(secrets.token_urlsafe(48))"

# 3. Старт
python -m web                       # → http://127.0.0.1:8080
```

Файл `.env` читається **автоматично** (і на Windows теж) — вручну експортувати
змінні не треба. При старті видно, що саме підхопилося:

```
📄 Прочитано налаштування з C:\...\ostvytsya\.env
🔑 OPENAI_API_KEY: sk-pro…(164 симв.)
🌐 Веб-квест на http://127.0.0.1:8080
```

Якщо бачиш `⚠️ Файл .env не знайдено` або `⚠️ Не задано OPENAI_API_KEY` —
перевір, що файл названо саме `.env` (Провідник Windows любить дописати `.txt`)
і лежить у корені проєкту.

Відкрий у браузері, увійди — і далі:

| Що | Де |
|---|---|
| Список персонажів | головна `/` |
| Створити персонажа | `+ Новий персонаж` |
| Змінити назву, промпт, голос, швидкість | `Редагувати` |
| Скопіювати персонажа з усім сценарієм | `Клонувати` → одразу відкриється копія |
| Видалити | `Видалити` (з підтвердженням) |
| **Голосовий квест** | `▶ Запустити квест` → дозволь мікрофон |

Квест **починається сам**, щойно дано доступ до мікрофона — так, ніби діти
щойно промовили кодове слово. Персонаж заговорить першим.

**Перебити персонажа не можна:** поки він говорить, мікрофон вимкнено
(`🔇 Персонаж говорить — слухай`), а коли договорить — вмикається знову
(`🎙 Твоя черга — говори`). Це і правило гри, і захист від того, щоб модель
чула саму себе з колонок.

### 📡 Термінали

Сторінка **`/agents`** показує всі мобільні термінали, що звітують у цю панель:
ідентифікатор, чи йде зараз квест, модель телефону, заряд (свій і
Bluetooth-пристроїв), координати та коли був останній звіт. Термінал, від якого
нічого не чути понад 12 хвилин, показано приглушено.

### 🔄 Синхронізація персонажів між терміналами

Якщо в застосунку вказано адресу сервера (та сама, що для звітів), телефони
синхронізують персонажів через панель за правилом «останній запис перемагає»:
кожна правка персонажа (голос, промпт, кодові слова, швидкість мови…)
оновлює його таймстамп `updated_at`, одразу асинхронно летить на сервер
(`POST /api/characters-sync`) і за ≤5 хвилин з'являється на всіх інших
терміналах. Типові персонажі з комплекту мають `updated_at = 0`, тож
будь-яка правка на будь-якому телефоні їх переважує.

Протокол навмисно без станів: телефон щотакту шле повний набір своїх
персонажів, сервер лишає собі новіші й відповідає тими, що новіші за
телефонні. Тому система самовідновлюється після будь-чого — рестарту чи
ефемерного диска сервера (перший же такт будь-якого телефона наповнює
сховище знову), втрачених запитів, довгого офлайну. Збій синхронізації
ніколи не чіпає квест: помилки тихо ігноруються до наступного такту.

Нові персонажі, створені на будь-якому телефоні, так само з'являються на
всіх інших (і на свіжовстановлених теж). Видалення синхронізується через
«надгробки» (`{id, deleted: true, updated_at}`) у тому самому LWW-потоці:
видалив на одному терміналі — зникне на всіх. Виняток — три незнищенні
персонажі парку (**Домовичок, Повітруля, Дерево**): їх можна лише
редагувати; пункт «Видалити» для них прихований, а сервер і застосунок
відкидають їхні надгробки навіть від телефонів зі старим APK.

Щоб термінал з'явився у списку, у застосунку: **Налаштування → Звіти в панель**
— увімкнути й вписати адресу цього сервера у вигляді `https://host:port`.
Типово звітування **вимкнено**: доки його не ввімкнули, застосунок нікуди
нічого не надсилає.

Звіт іде раз на 5 хвилин, у фоні, і ні на що не впливає — якщо сервер
недоступний, квест працює як завжди.

Про що варто знати заздалегідь:

* **номер телефону** Android віддає лише тоді, коли його записав на SIM-карту
  оператор. Здебільшого цього немає, і колонка лишається порожньою;
* **заряд Bluetooth-пристрою** публічного API не має — читаємо прихований, який
  доступний не на всіх прошивках;
* **координати** беруться з останньої відомої точки (щоб звіт був миттєвим і
  не садив батарею) і потребують дозволу на геолокацію.

Реєстр терміналів живе **в пам'яті сервера**: після його перезапуску список
наповниться знову за кілька хвилин, історія не зберігається.

### Деплой (Render та інші хостинги)

У репозиторії є [`render.yaml`](render.yaml) — можна створити сервіс як Blueprint,
або налаштувати вручну:

| Параметр | Значення |
|---|---|
| Build command | `pip install -r requirements-web.txt` |
| Start command | `python -m web` |
| Health check path | `/api/health` |

Порт і адресу підбирати не треба: якщо платформа задає змінну `PORT` (Render,
Railway, Fly, Heroku), додаток сам слухає `0.0.0.0` на цьому порту. Локально
лишається `127.0.0.1:8080`, як і було.

Змінні оточення задай у панелі хостингу (`.env` там не потрібен):
`OPENAI_API_KEY`, `OSTVYTSYA_WEB_PASSWORD`, `OSTVYTSYA_WEB_SECRET`
(і `GEMINI_API_KEY`, якщо є персонажі з моделлю Google).

> ⚠️ Без `OSTVYTSYA_WEB_SECRET` кожен перезапуск сервісу вимикає всі сесії —
> користувачів викидатиме на сторінку входу. У `render.yaml` він генерується
> автоматично.

На хостингу сесійна кука автоматично позначається `Secure` (сайт за HTTPS), а
uvicorn запускається з `proxy_headers` — інакше за зворотним проксі ламаються
редиректи й WebSocket (`wss`) для Gemini-персонажів.

### Постійне сховище персонажів (Render Managed PostgreSQL)

**Проблема, яку це вирішує:** файлова система веб-сервісу на Render ефемерна.
Персонажі, що йдуть у репозиторії (`characters/*.yaml`), лишаються — вони
частина коду. А от усе, що створено чи відредаговано в браузері **після**
деплою, живе лише на диску інстансу й губиться при кожному передеплої (а
часом і при звичайному перезапуску).

**Рішення — опційне сховище в PostgreSQL**, перемикається одним прапорцем у
`config.yaml`:

```yaml
storage:
  render: no   # типово. yes → зберігати персонажів у Render Managed PostgreSQL
```

| `storage.render` | Де живуть персонажі | Переживає передеплой? |
|---|---|---|
| `no` (типово, або секції немає) | файли `characters/*.yaml` | ні (крім тих, що в репозиторії) |
| `yes` | Render Managed PostgreSQL (`DATABASE_URL`) | так |

**Якщо `no` або не задано — нічого не змінюється.** Жоден інший файл, поведінка
чи залежність не зачіпаються; це стосується лише веб-режиму. Консольна лялька
завжди читає персонажів зі свого пристрою, незалежно від
цього прапорця, — там файлова система постійна й PostgreSQL не потрібен.

**Увімкнення на Render:**

1. Прив'яжи Managed PostgreSQL до сервісу (Render підставить `DATABASE_URL`
   сам) — або через панель, або розкоментувавши блок `databases:` у
   [`render.yaml`](render.yaml).
2. Постав `storage.render: yes` у `config.yaml` і задеплой.

При першому зверненні до порожньої бази персонажі з `characters/*.yaml`
імпортуються туди автоматично (одноразово) — щоб Домовичок, Водяник,
Повітруля й Дерево не «зникли» одразу після ввімкнення.

**Локальний тест** (потрібен `pip install -r requirements-web.txt` — там уже є
`psycopg[binary]`, потрібен лише коли `storage.render: yes`):

```bash
# локальний PostgreSQL або будь-який хмарний — досить рядка з'єднання
echo "DATABASE_URL=postgresql://user:pass@localhost:5432/ostvytsya" >> .env
# і storage.render: yes у config.yaml
python -m web
# при старті побачиш:
#   🗄️  Сховище персонажів: Render Managed PostgreSQL (config.yaml → storage.render: yes)
```

Дані лежать в одній таблиці `ostvytsya_characters (id, data JSONB, updated_at)` —
міграцій, ORM чи ручного керування схемою не потрібно.

### Фонове зображення

Поклади `background.jpg` у **корінь проєкту** — і воно стане фоном усіх сторінок
(з розмиттям і теплою бежевою завісою, щоб текст читався). Підхоплюються також
`background.jpeg` і `background.png`. Файлу немає — сторінки просто лишаються з
градієнтним фоном, нічого не ламається.

### Модель і голоси

Для **кожного персонажа окремо** обирається, якою моделлю він говорить у
браузері (поле «Модель» у редакторі):

| Модель | Голоси | Як працює |
|---|---|---|
| **OpenAI Realtime** (`gpt-realtime-2.1-mini`) | `marin`, `cedar` (найякісніші), `alloy`, `ash`, `ballad`, `coral`, `echo`, `sage`, `shimmer`, `verse` | WebRTC напряму з браузера |
| **Google Gemini Live** | усі 30 офіційних: `Charon`, `Enceladus`, `Aoede`, `Leda`, `Laomedeia`, `Pulcherrima`, `Sadachbia`, `Sulafat`, `Zephyr`… (у редакторі — з описом характеру) | WebSocket через міст на сервері |

Список голосів у редакторі перемикається сам — видно лише той, що належить
обраній моделі. Швидкість мовлення (0.25–2.0) підтримує лише OpenAI.

Модель можна перевизначити змінними `OPENAI_REALTIME_MODEL` та
`GEMINI_LIVE_MODEL`. Консольна лялька завжди працює через Gemini, голосом
із поля `voice`.

> Персонажам із моделлю Google потрібні `GEMINI_API_KEY` і пакет `google-genai`
> (є в `requirements-web.txt`). Якщо працюєш лише з OpenAI — веб підніметься
> й без них.

### Мова та подача

До промпту **кожного** персонажа автоматично додаються два блоки
(див. `domovyk_quest/prompt.py`) — однаково для OpenAI й Gemini:

* **🇺🇦 мова** — говорити виключно українською, з питомою вимовою, без
  іноземного акценту. Голосові моделі OpenAI без цього читають українську
  з англійським акцентом;
* **🎭 подача** — грати голосом як актор: емоції, паузи, сміх, шепіт, казкова
  барвиста мова, а не рівний «дикторський» тон.

Писати це в кожному персонажі не треба — воно діє завжди й в обох режимах.

### Персонажі: сценарій чи вільний промпт

* **Наявні YAML-сценарії** (Домовичок, Повітруля…) працюють у вебі як є —
  інструкція збирається з `persona`/`style`/`questions`/`directives`.
  У редакторі можна подивитися, який саме промпт іде в модель.
* **Нові персонажі** зручно описати вільним текстом у полі «Промпт персонажа».
  Якщо це поле заповнене — воно **повністю замінює** зібраний сценарій.

Редагування у вебі **зберігає коментарі й форматування** YAML: правляться лише
ті поля, які ти справді змінив, решта файлу лишається недоторканою.

### Безпека

* доступ лише за логіном і паролем; пароль зберігається як `pbkdf2_sha256`-хеш
  (можна одразу покласти хеш: `python -m web --hash-password "пароль"`);
* ключ OpenAI не потрапляє в браузер — лише короткоживучий ephemeral-токен;
* промпт і голос задає сервер, клієнт їх не підмінить;
* форми й API захищені CSRF-токеном, кука — `SameSite=lax`.

> ⚠️ Мікрофон у браузері доступний лише на `https://` або `localhost`. Якщо
> відкриваєш з іншої машини — постав HTTPS (напр. nginx + Let's Encrypt) і
> увімкни `https_only=True` для куки у `web/app.py`.

---

## Додати нового персонажа

1. `cp characters/domovychok.yaml characters/berehynia.yaml`
2. Зміни `id`, `display_name`, `voice`, `wake_words`, `win_word`, `persona`, три `questions`.
3. Запусти: `python -m domovyk_quest --character characters/berehynia.yaml`

Кожна лялька = свій Pi зі своїм файлом персонажа (або один Pi, який запускають із
потрібним `--character`).

---

## Вартість і приватність

- У режимі **`vosk`** до кодового слова **нічого** не йде в хмару — лялька слухає
  локально. Витрати з'являються лише під час самого квесту (стрім у Gemini Live).
- У режимі **`gemini`** аудіо стрімиться постійно (дорожче для цілодобової роботи).
- Для дитячої інсталяції рекомендовано `vosk` + розумні тайм-аути (вже виставлені).

---

## Розв'язання проблем

| Симптом | Що робити |
|---|---|
| **Веб:** сторінка входу каже «пароль не налаштовано» | додай `OSTVYTSYA_WEB_PASSWORD` у `.env` і перезапусти `python -m web` |
| **Веб:** «Не задано OPENAI_API_KEY», хоча ключ у файлі є | ключ вписано у `.env.example` замість `.env`, або файл названо `.env.txt`. Перевір рядок `📄 Прочитано налаштування з …` при старті — якщо його немає, файл не знайдено |
| **Веб:** «не вдалося отримати токен» / 502 | перевір `OPENAI_API_KEY` та баланс акаунта OpenAI; текст помилки від OpenAI видно в логу сервера |
| **Веб:** браузер не питає мікрофон | мікрофон дозволений лише на `https://` або `localhost` — постав HTTPS чи відкривай локально |
| **Веб:** «Недійсний CSRF-токен» | сторінка провисіла з попереднього запуску сервера — просто онови її (F5) |
| **Веб:** персонажі, створені в браузері, зникають після передеплою на Render | файлова система хостингу ефемерна. Увімкни постійне сховище: `storage.render: yes` у `config.yaml` + прив'яжи Managed PostgreSQL — див. розділ вище |
| **Веб:** `storage.render: yes потребує пакет psycopg` | забув `pip install -r requirements-web.txt` після оновлення (там уже є `psycopg[binary]`) |
| **Веб:** `не задано DATABASE_URL` при `storage.render: yes` | прив'яжи Managed PostgreSQL до сервісу в Render (підставить сам) або впиши рядок з'єднання в `.env` для локального тесту |
| **Термінали:** застосунок не з'являється на `/agents` | у застосунку ввімкни «Звіти в панель» і перевір адресу — має бути з протоколом, `https://host:port`. Перший звіт іде одразу після збереження налаштувань |
| **Термінали:** порожні координати чи номер телефону | дозвіл на геолокацію не наданий; номер Android віддає, лише якщо його записав на SIM оператор — здебільшого його там немає |
| **Термінали:** правка персонажа не з'явилась на інших телефонах | на ОБОХ телефонах має бути вказана адреса сервера; зачекай до 5 хвилин (такт синхронізації) і перевір, що телефони мають інтернет |
| **Мобільний:** у діагностиці «З'єднання з Gemini відновлено (goAway…)» | це норма: одне WebSocket-з'єднання Live API живе ~10 хвилин, застосунок сам перепід'єднується з handle відновлення сесії, і розмова триває. Те саме при короткому зникненні мережі (wifi ↔ 4g). Квест завершується лише якщо 5 спроб поспіль не вдалися |
| **Веб:** після перезапуску сервера викидає на логін | не заданий `OSTVYTSYA_WEB_SECRET` — без нього ключ підпису куки щоразу новий |

---

*Зроблено для парку «Оствиця». Слава кмітливим мандрівникам! 🛶*
