# Залежності ВЕБ-режиму (OpenAI Realtime). Консольний режим їх не потребує —
# для нього достатньо requirements.txt.
#
#   pip install -r requirements-web.txt

fastapi>=0.115          # веб-каркас
uvicorn[standard]>=0.30 # ASGI-сервер
jinja2>=3.1             # шаблони сторінок
python-multipart>=0.0.9 # розбір HTML-форм (логін)
itsdangerous>=2.1       # підпис сесійної куки
httpx>=0.27             # запити до OpenAI (ephemeral-токени)
PyYAML>=6.0             # читання/запис персонажів
ruamel.yaml>=0.18       # редагування YAML зі збереженням коментарів
google-genai>=1.16.0    # Gemini Live для веб-режиму (провайдер google)
